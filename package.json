{
  "name": "@whatskit/kill-port",
  "version": "0.1.0",
  "description": "Kill the process running on given port",
  "license": "MIT",
  "main": "index.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/whatskit/kill-port.git"
  },
  "homepage": "https://github.com/whatskit/kill-port",
  "bugs": "https://github.com/whatskit/kill-port/issues",
  "author": "Billgo",
  "bin": {
    "kill-port": "cli.js"
  },
  "scripts": {
    "start": "npm run dev",
    "pretest": "npm run lint",
    "test": "jest --env=node",
    "test:watch": "npm test -- --watch",
    "coverage": "npm test -- --coverage",
    "lint": "standard --fix",
    "precommit": "npm test",
    "check": "npm-check -u"
  },
  "keywords": [
    "port",
    "process",
    "kill",
    "kill-port",
    "port"
  ],
  "dependencies": {
    "get-them-args": "1.3.2",
    "shell-exec": "1.0.2"
  },
  "devDependencies": {
    "husky": "4.2.5",
    "jest": "26.1.0",
    "npm-check": "^5.9.2",
    "standard": "14.3.4"
  },
  "standard": {
    "env": {
      "jest": true
    }
  }
}
